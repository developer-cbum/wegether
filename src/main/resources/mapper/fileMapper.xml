<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wegether.app.mapper.FileMapper">
    <select id="selectAll" resultType="fileVO">
        SELECT ID, FILE_PATH, FILE_UUID, FILE_NAME, FILE_SIZE, FILE_TYPE
        FROM TBL_FILE
    </select>

<!--    <select id="select" resultType="fileVO">-->
<!--        SELECT ID, FILE_PATH, FILE_UUID, FILE_NAME, FILE_SIZE, FILE_TYPE-->
<!--        FROM TBL_PRODUCT-->
<!--        WHERE ID = #{id}-->
<!--    </select>-->


    <insert id="insert">
        <selectKey keyProperty="id" order="BEFORE" resultType="Long">
            SELECT SEQ_FILE.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_FILE
        (ID, FILE_PATH, FILE_UUID, FILE_NAME, FILE_SIZE)
        VALUES(#{id}, #{filePath}, #{fileUuid}, #{fileName}, #{fileSize})
    </insert>

    <select id="selectAll" resultType="communityFileDTO">
        SELECT F.ID, FILE_PATH, FILE_UUID, FILE_NAME, FILE_SIZE, COMMUNITY_ID
        FROM TBL_FILE F JOIN TBL_COMMUNITY_FILE C
        ON F.ID = C.ID AND COMMUNITY_ID = #{communityId}
    </select>
</mapper>