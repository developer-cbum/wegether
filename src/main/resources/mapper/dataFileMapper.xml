<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wegether.app.mapper.DataFileMapper">

    <select id="selectAll" resultType="dataFileVO">
        SELECT FILE_ID, DATA_ID FROM TBL_DATA_FILE WHERE DATA_ID = #{dataId}
        ORDER BY DATA_ID
    </select>

    <select id="select" resultType="dataFileVO">
        SELECT ID, FILE_PATH, FILE_UUID, FILE_NAME, FILE_SIZE, FILE_TYPE
        FROM TBL_PRODUCT
        WHERE ID = #{id}
    </select>

    <insert id="insert">
        INSERT INTO TBL_FILE
        (ID, FILE_PATH, FILE_UUID, FILE_NAME, FILE_SIZE, POST_ID, FILE_TYPE)
        VALUES(SEQ_FILE.NEXTVAL, #{filePath}, #{fileUuid}, #{fileName}, #{fileSize}, #{postId}, #{fileType})
    </insert>

    <delete id="delete">
        DELETE FROM TBL_FILE WHERE ID = #{id}
    </delete>

    <delete id="deleteAll">
        DELETE FROM TBL_FILE WHERE DATA_ID = #{dataId}
    </delete>
</mapper>